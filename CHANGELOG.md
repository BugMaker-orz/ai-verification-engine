# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 规范。

## [2.1.1] - 2026-09-08

### 修复
- **AI 补位建议崩溃**：`generate_remedy_suggestion` 提示词中误用未定义变量 `rule.name`，启用 AI 后生成补位建议时必然抛 `NameError: name 'rule' is not defined`，已改为 `rule_name`

### 测试
- 新增 7 项 AI 语义链路测试（补位建议/字段提取/条款匹配/冲突排除/总体摘要），覆盖 AI 启用与未启用两种路径

## [2.1.0] - 2026-09-08

### 修复
- **文档标题误报**：字段提取改用 MULTILINE 匹配，`^…$` 锚点按行生效；标题正则允许"合同/协议"收尾（含"合同书/协议书"）。修复"软件开发服务合同""房屋租赁合同"等常见标题全部匹配不到、被误报"缺少文档标题"的问题
- **HTML 报告注入风险**：HTML 报告启用 Jinja2 自动转义（autoescape），文档内容中的脚本/标签不再原样注入报告页面
- **评分模型**：扣分按文档数归一化，多文档问题叠加不再直接把评分打到 0（样例从 0/F 提升至 60/D 量级）
- **占位符规则收紧**：`【.*?】` 改为只匹配空白占位或含"待填写/待补充/TODO"等关键词的方括号，正常方括号标注（如"【合同金额】"）不再误报

### 技术
- 新增 `AI-Verification-Engine.spec`：PyInstaller 打包配置，`collect_all` 批量收集 30+ 依赖包的数据文件，解决 exe 运行时缺 `version.txt` 等数据文件的问题
- `build_exe.bat`：自动检测 `py`/`python` 启动器，纯英文输出规避 cmd 编码乱码

## [2.0.0] - 2026-09-08

### 新增
- **AI 语义增强**：支持接入大模型 API 做语义级分析
  - 语义级条款匹配（同义表述识别）
  - 冲突误报排除（AI 二次确认）
  - AI 字段提取增强（正则兜底）
  - AI 补位建议（根据文档内容生成具体建议）
  - AI 总体评价（3-5 句审查结论）
- **AI 客户端模块** (`src/ai_client.py`)：OpenAI 兼容接口封装，支持所有主流 AI 服务
- **语义理解模块** (`src/semantic.py`)：基础规则 + AI 双模式统一接口
- **Web 界面 AI 设置面板**：可配置 base_url/api_key/model，测试连接，保存配置
- **CLI AI 参数**：`--ai-base-url` / `--ai-key` / `--ai-model` / `--ai-config`
- **自动降级机制**：未配置 AI 或 AI 调用失败时自动回退基础规则匹配
- **文档**：新增 AI 配置指南、规则库编写指南

### 变更
- 引擎 `VerificationEngine` 构造函数新增 `ai_config` 参数
- `extractor.extract_all` / `conflict.detect_conflicts` / `gap.check_all` 均新增 `ai_config` 参数
- 报告头部新增"检查模式"显示（AI 增强 / 基础匹配）
- AI 模式下报告新增"AI 总体评价"段落
- 欠缺项新增 `match_method` 字段（关键词/正则/AI语义）
- README 全面更新，项目结构更新

### 技术
- 新增依赖：`requests`（AI API 调用）
- 不依赖 openai SDK，纯 requests 调用，轻量通用

## [1.2.0] - 2026-09-08

### 新增
- **Web 图形界面** (`app.py`)：基于 Gradio，支持拖拽上传、可视化结果、报告下载
- **零基础使用指南** (`docs/零基础使用指南.md`)：面向非技术人员的完整使用教程
- Web 界面结果区：评分展示、冲突列表、欠缺列表、补位建议、报告下载

### 变更
- README 加入 Web 界面使用方法（作为推荐方式）
- 项目结构更新

### 技术
- 新增依赖：`gradio`

## [1.1.0] - 2026-09-08

### 新增
- 命令行入口 (`cli.py`)：支持 `-r` / `-f` / `-o` / `-n` / `--quiet` 参数
- 非零退出码：有 high 及以上问题时返回 exit code 2
- 终端摘要打印 (`print_summary`)

### 变更
- 引擎 API 稳定化

## [1.0.0] - 2026-09-08

### 新增
- **核心引擎**：文件解析 → 字段提取 → 冲突检测 → 欠缺检查 → 报告生成
- **多格式解析**：PDF (pdfplumber) / DOCX (python-docx) / TXT
- **规则库系统**：YAML 格式，支持字段规则、必含条款、禁止条款、跨文档规则
- **通用合同规则集** (`rules/general_contract.yaml`)：10 字段 + 6 必含条款 + 3 禁止条款 + 6 跨文档规则
- **冲突检测**：数值容差、日期归一化、文本归一化
- **欠缺检查**：必填字段缺失、格式不合法、必含条款缺失、禁止条款出现
- **验真评分**：100 分制 + A-F 等级，加权扣分
- **双格式报告**：Markdown（可编辑）+ HTML（可视化）
- **示例文档**：主合同 + 补充协议（含故意制造的冲突与欠缺）
- **测试**：7 项测试用例（规则加载/文件解析/字段提取/冲突逻辑/冲突检测/欠缺检查/端到端）
- **技术架构文档** (`docs/architecture.md`)

---

## 版本说明

- **主版本号**：不兼容的 API 变更
- **次版本号**：向下兼容的功能性新增
- **修订号**：向下兼容的问题修正

### 已完成路线图
- [x] v1.0 核心引擎 + 规则库 + 报告
- [x] v1.1 命令行工具
- [x] v1.2 Web 图形界面
- [x] v2.0 AI 语义增强

### 规划中
- [ ] 规则库可视化编辑器
- [ ] Excel/CSV 表格类文件解析
- [ ] 报告导出 PDF
- [ ] 多语言规则库（英文合同）
- [ ] 跨字段逻辑校验
- [ ] OCR 集成（扫描件 PDF）
